Bash script to change mouse position while screen goes idle.

Install xautomation and xprintidle.

On Ubuntu,
 
   sudo apt-get install xautomation xprintidle
   sudo apt-get install python-appindicator python-xlib
